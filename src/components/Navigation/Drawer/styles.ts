import { CSSProperties } from "react"
import theme from "../../../mui-theme"

export const disabledLink: CSSProperties = {
  pointerEvents: "none",
  color: "white",
}

export const iconColor: CSSProperties = {
  color: theme.palette.info.main,
}
